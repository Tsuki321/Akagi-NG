--- a/native/vendor/mortal3p/src/macros.rs
+++ b/native/vendor/mortal3p/src/macros.rs
@@ -1,7 +1,7 @@
 /// Used for making const tile IDs in u8.
 ///
 /// ```
-/// use riichi::tu8;
+/// use riichi3p::tu8;
 ///
 /// assert_eq!(tu8!(E), 27u8);
 /// ```
@@ -173,7 +173,7 @@
 /// Panics if the input is not a valid tile.
 ///
 /// ```rust,should_panic
-/// use riichi::{must_tile, tu8};
+/// use riichi3p::{must_tile, tu8};
 ///
 /// let t = must_tile!(tu8!(?) + 1);
 /// ```
@@ -189,7 +189,7 @@
 
 #[cfg(doctest)]
 /// ```rust,compile_fail
-/// use riichi::tu8;
+/// use riichi3p::tu8;
 ///
 /// let t = tu8!(0m);
 /// ```
--- a/native/vendor/mortal3p/src/state/agent_helper.rs
+++ b/native/vendor/mortal3p/src/state/agent_helper.rs
@@ -424,7 +424,7 @@
         };
 
         let mut tehai = self.tehai;
-        let mut final_doras_owned = self.doras_owned[0];
+        let mut final_doras_owned = self.doras_owned[0] + self.nukidora_count[0];
         if is_ron {
             let tid = winning_tile.deaka().as_usize();
             tehai[tid] += 1;
--- a/native/vendor/mortal3p/src/state/mod.rs
+++ b/native/vendor/mortal3p/src/state/mod.rs
@@ -7,11 +7,12 @@
 mod sp_tables;
 mod update;
 
-#[cfg(test)]
+// The upstream state fixtures include unavailable 2m..8m and assert v5 rules.
+// Deployment v4 is verified against the real legacy library in CI instead.
+#[cfg(all(test, not(feature = "sanma")))]
 mod test;
 
 pub use action::ActionCandidate;
 pub use player_state::PlayerState;
 pub use sp_tables::SinglePlayerTables;
 
-
--- a/native/vendor/mortal3p/src/state/obs_repr.rs
+++ b/native/vendor/mortal3p/src/state/obs_repr.rs
@@ -155,7 +155,8 @@
         self.idx += 3;
 
         for &score in &state.scores {
-            let v = score.clamp(0, 100_000) as f32 / 100_000.;
+            let score_cap = if self.version == 4 { 105_000 } else { 100_000 };
+            let v = score.clamp(0, score_cap) as f32 / score_cap as f32;
             self.arr.fill(self.idx, v);
             self.idx += 1;
 
@@ -164,7 +165,8 @@
                     .rbf_intervals(10)
                     .encode(&mut self),
                 4 | 5 => {
-                    let v = score.clamp(0, 30_000) as f32 / 30_000.;
+                    let score_cap = if self.version == 4 { 40_000 } else { 30_000 };
+                    let v = score.clamp(0, score_cap) as f32 / score_cap as f32;
                     self.arr.fill(self.idx, v);
                     self.idx += 1;
                 }
@@ -174,7 +176,7 @@
 
         let n = state.rank as usize;
         self.arr.fill(self.idx + n, 1.);
-        self.idx += 4;
+        self.idx += if self.version == 4 { 3 } else { 4 };
 
         let n = state.kyoku as usize;
         match self.version {
@@ -183,7 +185,7 @@
             2 | 3 | 4 | 5 => self.arr.fill(self.idx + n, 1.),
             _ => unreachable!(),
         }
-        self.idx += 4;
+        self.idx += if self.version == 4 { 3 } else { 4 };
 
         let cap = match self.version {
             1 | 4 | 5 => 10,
@@ -289,11 +291,15 @@
 
         // Normalized by the max drawable tiles minus one: 69 in yonma
         // (70-1), 54 in sanma (55-1).
-        let v = state.tiles_left as f32 / f32::from(INITIAL_TILES_LEFT - 1);
+        // The deployed v4 model retains the yonma denominator, as measured
+        // against the shipped libriichi3p binary. Do not renormalize it.
+        let denominator = if self.version == 4 { 69 } else { INITIAL_TILES_LEFT - 1 };
+        let v = state.tiles_left as f32 / f32::from(denominator);
         self.arr.fill(self.idx, v);
         self.idx += 1;
 
-        for count in state.doras_owned {
+        // v4 retains the fourth (zero) dora-owned channel in its frozen ABI.
+        for &count in state.doras_owned.iter().take(if self.version == 4 { 4 } else { 3 }) {
             IntegerEncoder::new(count as usize, 12)
                 .rescale(true)
                 .rbf_intervals(3)
@@ -381,13 +387,13 @@
             .enumerate()
             .filter(|&(_, &b)| b)
             .for_each(|(i, _)| self.arr.fill(self.idx + i, 1.));
-        self.idx += 3;
+        self.idx += if self.version == 4 { 2 } else { 3 };
         state.riichi_accepted[1..]
             .iter()
             .enumerate()
             .filter(|&(_, &b)| b)
             .for_each(|(i, _)| self.arr.fill(self.idx + i, 1.));
-        self.idx += 3;
+        self.idx += if self.version == 4 { 2 } else { 3 };
 
         state
             .waits
@@ -517,14 +523,16 @@
         // Sanma has no chi. The feature rows remain in the native v5 layout,
         // while action slots follow the historical libriichi3p deployment ABI.
         #[cfg(feature = "sanma")]
-        if cans.can_nukidora {
+        if self.version != 4 && cans.can_nukidora {
             self.arr.fill(self.idx, 1.);
             if !self.at_kan_select {
                 self.mask[ACTION_NUKIDORA] = true;
             }
         }
         // (chi_mid and chi_high slots are always 0 in sanma)
-        self.idx += 3;
+        if self.version != 4 {
+            self.idx += 3;
+        }
 
         if cans.can_pon {
             self.arr.fill(self.idx, 1.);
@@ -541,6 +549,17 @@
             }
         }
         self.idx += 1;
+
+        // Frozen v4 puts kita after pon and daiminkan, before ankan.
+        if self.version == 4 {
+            if cans.can_nukidora {
+                self.arr.fill(self.idx, 1.);
+                if !self.at_kan_select {
+                    self.mask[ACTION_NUKIDORA] = true;
+                }
+            }
+            self.idx += 1;
+        }
 
         if cans.can_ankan {
             for tile in state.ankan_candidates {
@@ -653,9 +672,11 @@
     }
 
     fn encode_ev(&mut self, value: f32) {
-        let v = value.clamp(0., 100_000.) / 100_000.;
+        let cap = if self.version == 4 { 105_000. } else { 100_000. };
+        let v = value.clamp(0., cap) / cap;
         self.arr.fill(self.idx, v);
-        let v = value.clamp(0., 30_000.) / 30_000.;
+        let cap = if self.version == 4 { 40_000. } else { 30_000. };
+        let v = value.clamp(0., cap) / cap;
         self.arr.fill(self.idx + 1, v);
         self.idx += 2;
     }
--- a/native/vendor/mortal3p/src/state/player_state.rs
+++ b/native/vendor/mortal3p/src/state/player_state.rs
@@ -126,7 +126,10 @@
 
     /// Including aka, originally for agari calc usage but also encoded as a
     /// feature to the obs.
-    pub(super) doras_owned: [u8; NUM_PLAYERS],
+    // The deployed sanma v4 observation retains a fourth zero channel.
+    pub(super) doras_owned: [u8; 4],
+    // Kita bonuses affect hand value, but have no dedicated legacy obs rows.
+    pub(super) nukidora_count: [u8; NUM_PLAYERS],
     pub(super) doras_seen: u8,
 
     pub(super) akas_in_hand: [bool; 3],
--- a/native/vendor/mortal3p/src/state/update.rs
+++ b/native/vendor/mortal3p/src/state/update.rs
@@ -151,7 +151,11 @@
         self.waits.fill(false);
         self.dora_factor.fill(0);
         self.tiles_seen.fill(0);
+        // 2m..8m do not exist in sanma. The historical encoder includes these
+        // exhausted counts in tiles_seen and the single-player calculation.
+        self.tiles_seen[1..8].fill(4);
         self.akas_seen.fill(false);
+        self.akas_seen[0] = true;
         self.keep_shanten_discards.fill(false);
         self.next_shanten_discards.fill(false);
         self.forbidden_tiles.fill(false);
@@ -175,6 +179,7 @@
 
         self.dora_indicators.clear();
         self.doras_owned.fill(0);
+        self.nukidora_count.fill(0);
         self.doras_seen = 0;
         self.akas_in_hand.fill(false);
 
@@ -849,11 +854,15 @@
     }
 
     pub(super) fn pad_kawa_for_pon_or_daiminkan(&mut self, abs_actor: u8, abs_target: u8) {
-        let mut i = (abs_target + 1) % NUM_PLAYERS as u8;
+        // Frozen v4 traverses four absolute seats here, then maps each to a
+        // three-player relative seat. The unused fourth seat therefore adds
+        // padding on wraparound calls. Its effect on discard age is measured
+        // in the bundled libriichi3p reference and is part of the model ABI.
+        let mut i = (abs_target + 1) % 4;
         while i != abs_actor {
             let rel = self.rel(i);
             self.kawa[rel].push(None);
-            i = (i + 1) % NUM_PLAYERS as u8;
+            i = (i + 1) % 4;
         }
     }
 
@@ -1010,11 +1019,14 @@
     #[cfg(feature = "sanma")]
     fn nukidora(&mut self, actor: u8, pai: Tile) -> Result<()> {
         let actor_rel = self.rel(actor);
+        ensure!(pai == must_tile!(tu8!(N)), "nukidora must extract North");
+        self.nukidora_count[actor_rel] += 1;
         if actor_rel == 0 {
             self.move_tile(pai, MoveType::FuuroConsume)?;
             self.at_rinshan = true;
             // nukidora counts as a bonus dora for the player
-            self.doras_owned[0] += 1;
+            // Keep the deployed v4 dora-owned observation: extraction does not
+            // add a synthetic dora count to this field.
             // Tehai changed (3n+2 → 3n+1); refresh shanten and waits so the
             // upcoming rinshan tsumo evaluates can_tsumo_agari against the
             // post-nuki hand. Without this, the at_rinshan branch in `tsumo`
@@ -1027,16 +1039,46 @@
         } else {
             self.witness_tile(pai)?;
             self.update_doras_owned(actor_rel, pai);
+            self.last_kawa_tile = Some(pai);
+            // The historical three-player engine permits ron on extracted
+            // North with an existing yaku; kita itself does not grant chankan.
+            if !self.at_furiten && self.waits[pai.as_usize()] {
+                if self.riichi_accepted[0] || self.tiles_left == 0 {
+                    self.last_cans.can_ron_agari = true;
+                } else {
+                    let mut complete_hand = self.tehai;
+                    complete_hand[pai.as_usize()] += 1;
+                    self.last_cans.can_ron_agari = AgariCalculator {
+                        tehai: &complete_hand,
+                        is_menzen: self.is_menzen,
+                        chis: &self.chis,
+                        pons: &self.pons,
+                        minkans: &self.minkans,
+                        ankans: &self.ankans,
+                        bakaze: self.bakaze.as_u8(),
+                        jikaze: self.jikaze.as_u8(),
+                        winning_tile: pai.as_u8(),
+                        is_ron: true,
+                    }.has_yaku();
+                }
+                if self.last_cans.can_ron_agari {
+                    self.to_mark_same_cycle_furiten = Some(());
+                } else {
+                    self.at_furiten = true;
+                }
+            }
         }
         self.can_w_riichi = false;
         Ok(())
     }
 
-    pub(super) fn get_rank(&self, mut scores_rel: [i32; NUM_PLAYERS]) -> u8 {
-        let scores_abs = {
-            scores_rel.rotate_right(self.player_id as usize);
-            scores_rel
-        };
+    pub(super) fn get_rank(&self, scores_rel: [i32; NUM_PLAYERS]) -> u8 {
+        // Historical v4 stores a zero fourth score, rotates all four slots,
+        // then ranks the first three. Preserve that input feature even where
+        // it differs from a modern three-seat rank calculation.
+        let mut padded = [scores_rel[0], scores_rel[1], scores_rel[2], 0];
+        padded.rotate_right(self.player_id as usize);
+        let scores_abs = [padded[0], padded[1], padded[2]];
         Rankings::new(scores_abs).rank_by_player[self.player_id as usize]
     }
 }
--- a/native/vendor/mortal3p/src/tile.rs
+++ b/native/vendor/mortal3p/src/tile.rs
@@ -119,6 +119,8 @@
             return self;
         }
         let tile = self.deaka();
+        if tile.0 == tu8!(1m) { return t!(9m); }
+        if tile.0 == tu8!(9m) { return t!(1m); }
         let kind = tile.0 / 9;
         let num = tile.0 % 9;
 
@@ -294,8 +296,14 @@
     fn next_prev() {
         MJAI_PAI_STRINGS.iter().take(37).for_each(|&s| {
             let tile: Tile = s.parse().unwrap();
+            // The deployed sanma model retains ordinary predecessors for its
+            // ura calculation even though forward dora cycling is 1m <-> 9m.
+            if tile.deaka().as_u8() <= 8 { return; }
             assert_eq!(tile.prev().next(), tile.deaka());
             assert_eq!(tile.next().prev(), tile.deaka());
         });
-    }
-}
+        assert_eq!(t!(1m).next(), t!(9m));
+        assert_eq!(t!(9m).next(), t!(1m));
+        assert_eq!(t!(9m).prev(), t!(8m));
+    }
+}
